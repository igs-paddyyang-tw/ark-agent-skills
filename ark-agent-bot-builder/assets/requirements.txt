# 套件消費端只需裝 ark_bot_agent wheel（其餘依賴由套件帶入）。
#
# 🔴 一定要帶 extras —— 只裝 base wheel 會少兩組能力，而且**不會報錯**：
#   [search] bm25s / jieba / PyStemmer
#            → 缺了四層搜尋「靜默降級」成 purepy + CJK bigram
#   [skills] apscheduler / jinja2 / markdown / feedparser / bs4 / openai
#            → 缺 apscheduler 時 schedule_engine 只印一行 WARNING 就跳過
#
# 安裝：
#   uv venv --python 3.13
#   uv pip install --python .venv/bin/python '<ark_bot_agent-*.whl>[search,skills]'
#
# 換裝後驗 import 版號，**不看 pip 輸出**：
#   .venv/bin/python -c "import ark_bot_agent; print(ark_bot_agent.__version__)"
#
# 業務 skill 若有額外依賴，另建 skills/<name>/requirements.txt。
ark_bot_agent[search,skills]   # 由 wheel 安裝，不從 PyPI
